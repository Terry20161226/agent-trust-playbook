# 示例 05：一个语义散布三处

配套文章：[《1000+ 治理测试是怎么长出来的》](../../articles/05-治理测试是怎么长出来的.md)

```bash
node single-source-demo.mjs
```

同一条门禁规则（"交付必须含绑定指纹的测试凭证"）：

- 版本 A：规则散布在配置、硬编码校验、文档三处，只改一处 → 三个裁判三种说法；
- 版本 B：规则只存于单一事实源，校验器读取它、文档从它生成 → 矛盾在结构上不可能存在。

零依赖，Node 18+ 直接运行。
